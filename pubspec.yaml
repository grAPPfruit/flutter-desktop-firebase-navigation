name: checklist
description: Get a handle on your day.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.0.1+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^2.2.0
  bloc: ^7.0.0
  colorize_lumberdash: ^3.0.0
  fimber: ^0.6.1
  firedart: ^0.9.0+1
  flutter_bloc: ^7.0.1
  freezed_annotation: ^0.14.3
  get_it: ^7.2.0
  hive: ^2.0.4
  path_provider: ^2.0.2
  shared_preferences: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator: ^2.1.0
  build_runner: ^2.0.6
  flutter_lints: ^1.0.4
  freezed: ^0.14.2
  hive_generator: ^1.1.0

dependency_overrides:
  meta: 1.7.0

flutter:
  uses-material-design: true
